# When built and served from the backend on port 4000,
# the frontend and API are on the same origin — no need to hardcode an IP.
# Leave REACT_APP_API_URL empty so the app uses window.location.origin.
REACT_APP_API_URL=
